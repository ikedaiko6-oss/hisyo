# hisyo リポジトリ運用ルール

## タスク自動蓄積

会話の中でDaisukeが以下のような発言をしたら、明示的に「タスクに追加して」と言われなくても、`タスク管理統合版_20260619.md` に自動で追記し、コミット＆プッシュすること。

- 何かが完了した／進捗があった → 該当タスクを `[x]` にする、または完了タスクに追記
- 新しい依頼・案件・展開が発生した → 該当セクション（ビジネス・戦略／建設／開発／健康・農業／その他）に `[ ]` で追記
- 何かが停滞・保留になった → 既存タスクの末尾に「（停滞中）」等を追記

追記後は簡潔に「〇〇を追記しました」と報告する。逐一「追加していいですか？」と確認は不要（誤りがあれば後から訂正を頼まれる前提でよい）。

## 日次チェック（hisyo 毎日チェック Routine）

毎日17:45 JSTにこのセッションへ自動通知が来る。その際は：
1. `git -C /home/user/hisyo pull origin main`
2. `タスク管理統合版_20260619.md` を読んで、直近ホットな話題（最優先・継続タスク、直近数日で動きのあった案件、停滞中と書かれている項目など）を2〜3個ピックアップする
3. 「更新ありますか？」という一言で終わらせず、具体的に聞いて思い出しやすくする。ラリー（往復）で引き出すイメージ。例：
   - 「LINEスタンプの件、その後も広がってる？」
   - 「Xの投稿は続けられてる？」
   - 「うんこゲーム、何か動きあった？停滞中のままかな」
   - 「建設の方、2×4のカットとか進んだ？」
   毎回全項目を尋問のように聞く必要はない。2〜3個、自然な会話のトーンで聞く。答えが出たら「タスク自動蓄積」ルールに従ってその場で追記する。
4. 一通り聞いたら、他に追記したいことがないか最後に軽く聞いて締める
5. 更新があれば編集してコミット＆プッシュ

（ディレクトリが消えている場合は `git clone --depth 1 https://github.com/ikedaiko6-oss/hisyo /home/user/hisyo` で再クローンする）

## セッション健全性チェック（ターン数ベース）

このセッションは日次Routineで毎日同じ会話に追記され続けるため、放置すると際限なくコンテキストが肥大化する。日数ではなく「会話のやり取り量」で判断する。

- 目安：このセッション内の往復（ユーザー発言＋Claude応答）がおよそ**50ターン**を超えたら、日次チェックの返信の最後に一言、セッションを切り替えることを提案する。
  - 例：「そろそろ会話が長くなってきたので、新しいセッションに切り替えることをお勧めします。タスクやメモは全部hisyoリポジトリに保存済みなので、新しいセッションでこのCLAUDE.mdを読めば何も失わずに引き継げます。」
- 正確なターン数を数値管理する必要はない。会話履歴を振り返って「体感で50往復くらい経過していそうか」を判断すればよい。単純な日次「変更なし」チェックが続くだけの期間はターン消費が少ないので急かさなくてよいが、今回のように議論が続いた日はカウントが一気に進む点に注意。
- ユーザーが切り替えを了承したら、新しいセッションの初回でこのファイルとタスクファイルを読み直すだけで運用を再開できる。旧セッションは特にアーカイブ処理などしなくてよい。
